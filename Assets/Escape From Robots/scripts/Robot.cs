using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Robot : Damageable_Target
{
    // Start is called before the first frame update
    void Start()
    {
        // health = 100f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
